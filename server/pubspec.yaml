name: asset_sync_server
description: AssetTracker single-user sync server (LWW snapshot store).
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  sqlite3: ^2.4.0
dev_dependencies:
  test: ^1.31.2
