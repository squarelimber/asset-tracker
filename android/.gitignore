gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Release signing material is NOT committed. CI writes android/key.properties
# and android/release/*.keystore from repository secrets before building, so
# every release is signed with a stable, verifiable identity without keeping
# any key in the tree (see app/build.gradle.kts and the root .gitignore).
